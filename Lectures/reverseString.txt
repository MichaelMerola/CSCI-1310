string reverseString(string str)
{
    int Length = str.length();
    string strRev = "";
    
    for (int i = Length - 1; i >= 0; i--)
    {
        strRev += str[i];
    }
    
    return strRev;
    
}